{
  "name": "notes",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "JL",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ui": "rm -rf dist && cd ../phonebook-fe && npm run build && cp -r dist ../phonebook-be",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^9.1.6",
    "morgan": "^1.10.1"
  }
}
